public class Clean007 {
    public void readMax(int[] arr) {
        int max = arr.length;
        int idx = 0;
        while(idx < max) {
            System.out.println(arr[idx]);
            idx++;
        }
    }
}